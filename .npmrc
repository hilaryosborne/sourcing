# Hoist nothing implicitly — pnpm's strict default keeps phantom deps out, which
# is the hygiene we picked pnpm for. Publish/registry config (GitHub Packages,
# @hilaryosborne scope) is wired per package via publishConfig and in Epic 5.
auto-install-peers=true
