// Custom theme: the default VitePress theme + brand-color overrides in custom.css.
// Kept minimal on purpose — the polish is CSS variables, not bespoke components.
import DefaultTheme from "vitepress/theme";
import "./custom.css";

export default DefaultTheme;
